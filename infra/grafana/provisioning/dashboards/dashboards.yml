apiVersion: 1

providers:
  - name: SquadX
    orgId: 1
    folder: SquadX
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
