apiVersion: v1
kind: Secret
metadata:
  name: {{ include "squadx.fullname" . }}-secrets
  labels:
    {{- include "squadx.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
